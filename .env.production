# Next.js inlines NEXT_PUBLIC_* vars at build time, not at runtime - this must be set correctly
# BEFORE running `npm run build` for production. Setting it in ecosystem.config.js's env block
# would have no effect since PM2 only controls the already-built process's runtime env.
NEXT_PUBLIC_API_URL="https://pos-api.beautyup-enterprise.com"
